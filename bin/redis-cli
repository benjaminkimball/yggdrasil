#!/usr/bin/env bash

CONTAINER=`docker ps | grep -Eo '\w+-redis-\d+'`

source ./bin/signature
echo "============================================================"
echo "> Connecting to $CONTAINER..."
echo

docker exec -it $CONTAINER redis-cli
