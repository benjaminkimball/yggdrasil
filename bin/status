#!/usr/bin/env bash

source ./bin/signature

print_signature $*
echo "============================================================"
echo "> Inspecting background services..."
echo

docker compose ps -a
