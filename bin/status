#!/usr/bin/env bash

if [[ $* == *--no-signature* ]]; then
  echo
else
  source ./bin/signature
fi

echo "============================================================"
echo "> Inspecting background services..."
echo

docker compose ps -a
