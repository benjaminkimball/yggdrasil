#!/usr/bin/env bash

BLUE='\033[0;34m'
RESET='\033[0m'

echo
echo
printf "${BLUE}$(cat signature.txt)${RESET}"
echo
echo
echo "============================================================"
echo "> Cleaning up background service artifacts..."
echo

# docker rmi $(docker images -q)
docker volume rm $(docker volume ls -q)
