#!/usr/bin/env bash

BLUE='\033[0;34m'
RESET='\033[0m'

echo
echo
printf "${BLUE}$(cat bin/signature.txt)${RESET}"
echo
echo
echo "============================================================"
echo "> Cleaning up background services artifacts..."
echo

docker volume rm $(docker volume ls -q)
